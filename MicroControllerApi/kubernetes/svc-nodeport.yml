---
apiVersion: v1
kind: Service
metadata:
  name: iot-api-nodeport
  namespace: database
spec:
  type: NodePort
  selector:
    app: iot-api
  ports:
    - name: external
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30002
